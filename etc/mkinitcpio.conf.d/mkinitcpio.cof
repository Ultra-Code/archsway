# vim: set filetype=conf : #
# Drop in config file for mkinitcpio /etc/mkinitcpio.conf.d/mkinitcpio.conf
HOOKS=(base udev autodetect modconf kms keyboard keymap consolefont block filesystems resume fsck)
COMPRESSION="zstd"
COMPRESSION_OPTIONS=(-v -T0 --auto-threads=logical -12)
