alias ls='ls --color -F'
alias ll='ls -Alh'
alias li='ll -i'
alias lr='ll -R'

alias grep='grep --color'
alias fgrep='fgrep --color'
alias egrep='egrep --color'

alias casm="nasm -f elf32"
alias lasm="ld -m elf_i386"

alias admin='tmux new -s admin -n ADMINISTRATOR \; new-window -n EDITOR \; new-window -n DEVELOPER;'
alias dev='tmux new -s developer -n TESTING \; new-window -n DAEMON \; new-window -n DEVELOPER\; new-window -n DATABASE'

#Add hub as an alias for git hub
#So that git and git hub all have access to each other
#eval "$(hub alias -s)"
#alias __git=hub

#alias sv="source ~/.virtualevn/bin/activate"

alias gs='git status'
alias ga='git add'
alias gp='git push'
alias gtd='git tag --delete'
alias gr='git branch -r'
alias gpu='git pull'
alias gb='git branch '
alias gc='git commit'
alias gd='git diff'
alias gco='git checkout '
alias gr='git remote'
alias grs='git remote show'
alias gl='git log --graph --oneline --decorate'

# open ~/.zshrc in using the default editor specified in $EDITOR
alias ez="$EDITOR $HOME/.zshrc"

# source ~/.zshrc
alias sz="source $HOME/.zshrc"

#source ~/.zshenv
alias se="source $HOME/.zshenv"

#edit ~/.zshenv
alias ee="$EDITOR $HOME/.zshenv"

#source ~/.zsh_aliases
alias sa="source $HOME/.zsh_aliases"

#edit ~/.zsh_aliases
alias ea="$EDITOR $HOME/.zsh_aliases"

#Aptitude aliases
alias apu='sudo aptitude update'
alias aps='aptitude show'
alias apf='sudo aptitude full-upgrade'
alias api='sudo aptitude install'
alias apc='sudo aptitude autoclean'
alias app='sudo aptitude purge'
alias apr='sudo aptitude reinstall'
alias ap='aptitude'

#Cmake
alias cmgen='cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON'

#Clang
alias clf='ln -s ~/.clang* .'

#Build2
#alias bnew='bdep new -t empty'
#alias blib='bdep new --package -l c++ -t lib,split'
#alias bexe='bdep new --package -l c++ -t exe,prefix=src'
alias cldb='b -vn clean update |& compiledb'
alias bconr='bdep init -C ~/repos/c++/build2/build2builds/clang-release @clang-release cc config.cxx="clang++-11 -03" config.cxx.coptions="-Wall -Wextra -pedantic -pedantic-errors -Werror"'
alias bcond='bdep init -C ~/repos/c++/build2/build2builds/clang-debug @clang-debug cc config.cxx="clang++-11 -g -O0" config.cxx.coptions="-Weverything -Wno-c++98-compat -Wno-c++98-compat-pedantic -Wno-missing-prototypes"'
alias bcadd='bdep init -A ~/repos/c++/build2/build2builds/clang-debug'
alias bcadr='bdep init -A ~/repos/c++/build2/build2builds/clang-release'
alias bexe='bdep new -l c++ -t exe'
alias blib='bdep new -l c++ -t lib'

#Systemd services
alias enabled_services='systemctl list-unit-files --type=service --state=enabled'
alias running_services='systemctl list-units  --type=service  --state=running'
alias all_services='systemctl -at service'

#cppcheck
alias cppchecks='/usr/bin/cppcheck --enable=warning,style,performance,portability,information,missingInclude --inconclusive --force -j 4 --template=gcc --language=c++'
